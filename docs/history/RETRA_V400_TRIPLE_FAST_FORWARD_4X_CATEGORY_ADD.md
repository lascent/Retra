# Retra v4.00 — Triple Fast Forward + 4× Default + Categories Add Position

- Fast Forward now uses a centered three-chevron icon in the Screen Editor, Add Controller list, and native gameplay overlay.
- Fast-forward speed defaults to 4× for users who have not chosen a speed yet. Existing explicitly saved speed choices remain unchanged.
- Native gameplay fallback/default multiplier is also 4×.
- Categories `+ Add` button is moved slightly farther upward (38px from the bottom).
