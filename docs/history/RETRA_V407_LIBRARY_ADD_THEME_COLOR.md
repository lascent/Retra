# Retra v4.07 — Library Add Button Theme Color

- Removed the fixed violet color from the Library `+ Add ROM` floating button.
- The button now uses the active Appearance theme's `--accent` color.
- Added theme-aware border, gradient, press state, and icon contrast.
- Changing Appearance updates the Library add button automatically without restarting the app.
