# Retra v4.19 — Recent Saves Play Icon Fix

- Replaced the old save/load-style arrow in Recent Saves with a proper right-facing Play triangle.
- Centered the icon inside the circular action button.
- Added a tiny optical shift so the triangle looks visually centered.
- Existing tap-to-load and hold-to-delete behavior is unchanged.
